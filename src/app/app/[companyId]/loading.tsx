import { PageSkeleton } from "@/components/app-shell/page-skeleton";

/** Shown inside the shell while a company screen's data loads. */
export default function Loading() {
  return <PageSkeleton />;
}
